add = lambda x, y: x + y
print(f"Qosyndy: {add(5, 3)}")

square = lambda x: x ** 2
print(f"5-kv: {square(5)}")

numbers = [1, 2, 3, 4, 5]
squared_numbers = list(map(lambda x: x**2, numbers))
print(f"Kv tizim: {squared_numbers}")